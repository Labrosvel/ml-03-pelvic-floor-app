# Per-PR web preview for UI validation before merge.
# URL pattern: https://labrosvel.github.io/ml-03-pelvic-floor-app/pr-preview/pr-<N>/
name: preview-web

on:
  pull_request:
    types: [opened, reopened, synchronize, closed]

concurrency:
  group: pages-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  preview:
    name: Deploy PR web preview
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      # Always checkout: PR-close cleanup still needs a git working tree.
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node
        if: github.event.action != 'closed'
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        if: github.event.action != 'closed'
        run: npm ci

      - name: Export static web build
        if: github.event.action != 'closed'
        env:
          BASE_PATH: /ml-03-pelvic-floor-app/pr-preview/pr-${{ github.event.number }}
        run: |
          echo "export const WEB_BUILD_ID = 'pr-${{ github.event.number }}-${GITHUB_SHA::7}';" > constants/buildInfo.ts
          npx expo export -p web

      - name: Patch HTML cache headers
        if: github.event.action != 'closed'
        run: bash scripts/patch-web-cache-headers.sh dist

      - name: Ensure dist exists
        run: mkdir -p dist

      - name: Deploy or clean PR preview
        uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: ./dist
          umbrella-dir: pr-preview
